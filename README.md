XamppBranch Form1 - used MYSQL / XAMPP | simple CRUD METHOD

need to import Imports MySql.Data.MySqlClient

need to add nugetpackages/references - MySqlData

xampp branch uses crud metheod xampp mysql
